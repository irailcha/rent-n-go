// import { CarList } from "../CarList/CarList";

export const Favorite = ({ getFavorite }) => {
  return (
    <div>
      {/* {getFavorite.map((favorite) => (
        <CarList key={favorite.id} car={favorite} />
      ))} */}
    </div>
  );
};
